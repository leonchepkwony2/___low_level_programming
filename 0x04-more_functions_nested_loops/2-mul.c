#include "main.h"

/**
 * mul- multiplies two integers and returns the result
 * @a: first integer
 * @b: second integer
 * Return: result of multiplication operation
 */
int mul(int a, int b)
{
	return (a * b);
}
